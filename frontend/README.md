# Frontend - AI Chat Support

The frontend client for the Spur AI Chat application. A modern, responsive chat widget built with React and Vite.

## 🔧 Configuration

Create a `.env` file in this directory (`frontend/.env`) to configure the backend connection:

```env
# URL of the backend API
VITE_API_URL=http://localhost:4000
```

## 🚀 Scripts

- `npm install`: Install dependencies.
- `npm run dev`: Start the development server (default port 5173).
- `npm run build`: Build for production.
- `npm run preview`: Preview the production build locally.

## 📦 Tech Stack

- **React**: UI Library.
- **Vite**: Build tool and dev server.
- **TypeScript**: Static typing.
- **Axios**: HTTP client.
- **MUI (Material UI)**: UI Components and Icons.
